apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: unify-retail-agent
  annotations:
    kubernetes.io/ingress.class: gce
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: unify-retail-agent
                port:
                  number: 80

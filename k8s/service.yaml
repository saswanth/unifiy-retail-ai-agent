apiVersion: v1
kind: Service
metadata:
  name: unify-retail-agent
  labels:
    app: unify-retail-agent
spec:
  type: ClusterIP
  selector:
    app: unify-retail-agent
  ports:
    - name: http
      port: 80
      targetPort: 3000
